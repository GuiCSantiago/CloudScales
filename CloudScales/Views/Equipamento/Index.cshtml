@model List<EquipamentoViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Listagem de Equipamentos</h2>
<a href="/equipamento/create">Novo Registro</a>
<br />
<table class="table table-responsive">
    <tr>
        <th>Id</th>
        <th>CaminhaoID</th>
        <th>QtdBalanca</th>
    </tr>
    @foreach (var equip in Model)
    {
        <tr>
            <td>@equip.Id</td>
            <td>@equip.CaminhaoID</td>
            <td>@equip.QtdBalanca</td>
            <td>
                <a href="/equipamento/Edit?id=@equip.Id">Editar</a>
                <a href="javascript:apagarEquipamento(@equip.Id)">Apagar</a>
            </td>
        </tr>
    }
</table>
<script>
    function apagarEquipamento(id) {
        if (confirm('Confirma a exclusão do registro?'))
            location.href = 'equipamento/Delete?id=' + id;
    }
</script>
