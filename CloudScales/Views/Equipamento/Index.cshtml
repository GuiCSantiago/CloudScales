@model List<EquipamentoViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Listagem de Equipamentos</h2>
<a class="btn btn-outline-light" href="/equipamento/create">Novo Registro</a>
<br />
<br />
<table class="table table-responsive">
    <tr>
        <th>Id</th>
        <th>CaminhaoID</th>
        <th>QtdBalanca</th>
    </tr>
    @foreach (var equip in Model)
    {
        <tr>
            <td>@equip.Id</td>
            <td>@equip.CaminhaoID</td>
            <td>@equip.QtdBalanca</td>
            <td>
                <a class="btn btn-outline-warning" href="/equipamento/Edit?id=@equip.Id">Editar</a>
                <a class="btn btn-outline-danger" href="javascript:apagarEquipamento(@equip.Id)">Apagar</a>
            </td>
        </tr>
    }
</table>
<script>
    function apagarEquipamento(id) {
        if (confirm('Confirma a exclusão do registro?'))
            location.href = 'equipamento/Delete?id=' + id;
    }
</script>
