@model List<CelulaViewModel>

<table class="table table-responsive text-white">
    <tr>
        <th>EquipamentoID</th>
        <th>Peso</th>
        <th>Posicao</th>
    </tr>
    @foreach (var celula in Model)
    {
        <tr>
            <td>@celula.EquipamentoID</td>
            <td>@celula.Peso</td>
            <td>@celula.Posicao</td>
        </tr>
    }
    @if (Model.Count() == 0)
    {
        <tr>
            <td>
                Nenhum registro encontrado.
            </td>
        </tr>
    }
</table>